{
  "name": "forma-host-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:emu": "firebase --config ../firebase.json emulators:exec --only auth,firestore --project demo-forma \"vitest run --config vitest.emu.config.mts\""
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "nodemailer": "^10.0.10",
    "typescript": "~5.9.3",
    "vitest": "^5.0.2",
    "zod": "^4.6.5"
  }
}
